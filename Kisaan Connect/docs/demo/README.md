# KisanConnect Demo Scripts

Generated 2025-08-19. One folder per milestone with a `smoke.sh` script.

> Note: Scripts expect `jq` and `curl`. Some steps optionally use `mosquitto_pub` and ImageMagick `convert`.
